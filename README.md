The site addresses are:
laby : http://localhost:3000/api/auth
login : http://localhost:3000/api/auth/login
register : http://localhost:3000/api/auth/register
The rest can be accessed from the same pages.

The project uses the MongoDB database.
The database address is the same as its default.
Install the prerequisites with the command: npm install
