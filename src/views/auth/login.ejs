<div
  id="success-overlay"
  style="
    display: none;
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(255, 255, 255, 0.95);
    z-index: 9999;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    backdrop-filter: blur(10px);
  "
>
  <img
    src="https://arkca.com/assets/img/login.gif"
    alt="Success"
    style="width: 200px; border-radius: 20px"
  />
  <h2 style="color: #967e76; margin-top: 20px; font-family: 'Tahoma'">
    در حال انتقال به داشبورد...
  </h2>
</div>

<div class="auth-body">
  <div class="glass-card">
    <div class="character-container">
      <div class="avatar" id="avatar">
        <div class="hand left-hand"></div>
        <div class="hand right-hand"></div>
        <div class="face">
          <div class="eye left-eye"></div>
          <div class="eye right-eye"></div>
          <div class="nose"></div>
        </div>
      </div>
    </div>

    <h2 class="login-title">ورود به حساب</h2>

    <form
      id="loginForm"
      action="/api/auth/login"
      method="POST"
      onsubmit="handleLogin(event)"
    >
      <div class="input-group">
        <label>ایمیل یا نام کاربری</label>
        <input type="text" name="emailorname" required id="usernameInput" />
      </div>

      <div class="input-group">
        <label>رمز عبور</label>
        <input
          type="password"
          name="password"
          id="passInput"
          required
          onfocus="coverEyes()"
          onblur="uncoverEyes()"
        />
      </div>

      <button type="submit" class="glass-btn" id="submitBtn">ورود</button>

      <p class="auth-footer">
        حساب کاربری ندارید؟ <a href="/api/auth/register">ثبت‌نام کنید</a>
      </p>
    </form>
  </div>
</div>

<script>
  const avatar = document.getElementById("avatar");

  function coverEyes() {
    avatar.classList.add("covering");
  }

  function uncoverEyes() {
    avatar.classList.remove("covering");
  }

  function handleLogin(event) {
    event.preventDefault();
    const overlay = document.getElementById("success-overlay");
    const btn = document.getElementById("submitBtn");
    const form = document.getElementById("loginForm");

    btn.innerText = "لطفاً صبر کنید...";
    btn.style.opacity = "0.7";
    btn.disabled = true;

    overlay.style.display = "flex";

    setTimeout(() => {
      form.submit();
    }, 1800);
  }
</script>

<style>
  :root {
    --char-color: #967e76;
    --char-dark: #2d2424;
  }

  .auth-body {
    background: linear-gradient(45deg, #b7c4cf, #eee3cb, #d7c0ae, #967e76);
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: "Tahoma";
    direction: rtl;
    margin: 0;
  }

  .glass-card {
    background: rgba(255, 255, 255, 0.4);
    backdrop-filter: blur(10px);
    padding: 20px 40px 40px 40px;
    border-radius: 25px;
    width: 350px;
    text-align: center;
    border: 1px solid rgba(255, 255, 255, 0.3);
    position: relative;
    margin-top: 50px;
  }

  /* استایل‌های کاراکتر */
  .character-container {
    position: absolute;
    top: -80px;
    left: 50%;
    transform: translateX(-50%);
  }

  .avatar {
    width: 100px;
    height: 100px;
    background: var(--char-color);
    border-radius: 50% 50% 45% 45%;
    position: relative;
    border: 4px solid #fff;
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
    transition: all 0.3s ease;
  }

  .face {
    position: relative;
    width: 100%;
    height: 100%;
  }

  .eye {
    position: absolute;
    width: 12px;
    height: 12px;
    background: var(--char-dark);
    border-radius: 50%;
    top: 40px;
    transition: all 0.3s ease;
  }
  .left-eye {
    left: 25px;
  }
  .right-eye {
    right: 25px;
  }

  .nose {
    position: absolute;
    width: 10px;
    height: 8px;
    background: #55433c;
    border-radius: 50%;
    top: 55px;
    left: 50%;
    transform: translateX(-50%);
  }

  .hand {
    position: absolute;
    width: 35px;
    height: 35px;
    background: var(--char-color);
    border: 3px solid #fff;
    border-radius: 50%;
    bottom: -10px;
    transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    z-index: 10;
  }
  .left-hand {
    left: -10px;
  }
  .right-hand {
    right: -10px;
  }

  .avatar.covering .left-hand {
    transform: translate(30px, -50px);
  }
  .avatar.covering .right-hand {
    transform: translate(-30px, -50px);
  }
  .avatar.covering .eye {
    height: 2px;
    margin-top: 5px;
  }

  .login-title {
    color: var(--char-dark);
    margin-top: 40px;
  }
  .input-group {
    text-align: right;
    margin-bottom: 15px;
  }
  .input-group label {
    display: block;
    margin-bottom: 5px;
    color: #2d2424;
    font-size: 14px;
  }
  .input-group input {
    width: 100%;
    padding: 12px;
    border-radius: 10px;
    border: 1px solid #d7c0ae;
    outline: none;
    box-sizing: border-box;
    background: rgba(255, 255, 255, 0.8);
  }
  .glass-btn {
    width: 100%;
    padding: 15px;
    background: var(--char-color);
    color: #fff;
    border: none;
    border-radius: 12px;
    cursor: pointer;
    font-weight: bold;
    margin-top: 10px;
  }
  .glass-btn:hover {
    background: var(--char-dark);
  }
  .auth-footer {
    font-size: 13px;
    margin-top: 15px;
  }
</style>
